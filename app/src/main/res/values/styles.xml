<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="base_flag">
        <item name="android:layout_width">300dp</item>
        <item name="android:layout_height">200dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_margin">20dp</item>
    </style>

    <style name="vertical_flag" parent="base_flag">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="horizontal_flag" parent="base_flag">
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="editText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:background">@color/loginBg</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:textColorHint">@color/loginHint</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:drawablePadding">10dp</item>
        <item name="android:fontFamily">@font/monda_regular</item>
        <item name="android:textColor">@color/loginHint</item>

    </style>

    <style name="baseButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
        <item name="android:textColor">@color/colorHint</item>
        <item name="android:textSize">25sp</item>
    </style>


</resources>
